%h2 Visionaria
%div{:class => "tabs" }
    = link_to "VISIONARIAS", post_index_path, :method => :get, :class => "visions"
    = link_to "SUSTAINABLE UN GOALS", tagged_posts_path, :method => :get, :class => "goals"
%br
%br
%div{:class => "welcome_user"}
    - if user_signed_in?
        Welcome back, 
        = current_user.username