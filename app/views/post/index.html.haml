-#  This file is app/views/post/index.html.haml
= render 'shared/header'
%br
%br
- @posts.each do |p|
    = render 'shared/indiv_post', :post => p

- if user_signed_in?
    = link_to 'Make Post', new_post_path, :method => :get
    = link_to 'Logout', destroy_user_session_path, :method => :delete